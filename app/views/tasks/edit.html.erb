<h1 class="text-center my-4">Edit task</h1>
<div class="container">
  <div class="row">
    <div class="col-md-8 offset-md-2">
      <%= form_with model: @task, local: true do |form| %>
        <div class="mb-3">
          <%= form.label :title, class: "form-label" %>
          <%= form.text_field :title, class: "form-control" %>
        </div>
        <div class="mb-3">
          <%= form.label :details, class: "form-label" %>
          <%= form.text_area :details, class: "form-control" %>
        </div>
        <div class="form-check mb-3">
          <%= form.check_box :completed, class: "form-check-input" %>
          <%= form.label :completed, class: "form-check-label" %>
        </div>
        <%= form.submit 'Mettre à jour Tâche', class: "btn btn-primary" %>
      <% end %>
      <%= link_to 'Retour', tasks_path, class: "btn btn-secondary mt-3" %>
    </div>
  </div>
</div>
